@* Runs before every view in the app; setting Layout here means every view
   automatically gets wrapped in _Layout.cshtml without each view having to
   say so itself. *@
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
